const BASE_SIZE = 16
export enum LANG {
  cn,
  en,
}

export enum THEME {
  dark,
  light,
}

const COOKIE = {
  DEVICE: 'DEVICE',
  TOKENS: 'TKD98F0S8D0',
  DAY: 180,
}

export default {
  BASE_SIZE,
  LANG,
  COOKIE,
  LOADING: 'LOADING',
}
